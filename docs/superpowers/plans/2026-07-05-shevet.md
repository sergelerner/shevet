# SHEVET (שבט) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mobile-first RTL Hebrew static site on GitHub Pages that reads baby-gear recommendations live from a Google Sheet `DB` tab, plus a one-time migration script that flattens the existing messy tabs into that `DB` tab.

**Architecture:** No build step. `index.html` loads Preact + htm + @preact/signals as UMD `<script>` tags from jsDelivr. `lib.js` holds pure, node-testable functions (gviz parsing, row hygiene, grouping, search). `app.js` holds signals + components. A standalone Node script (`scripts/build-db-csv.mjs`) fetches all spreadsheet tabs via the public gviz endpoint and emits `data/db.csv` for one-time manual import into the sheet.

**Tech Stack:** Preact 10 UMD, htm 3 UMD, @preact/signals UMD, vanilla CSS, Node ≥18 (built-in `fetch`, `node:test`) for dev scripts/tests only. No npm, no package.json.

**Spec:** `docs/superpowers/specs/2026-07-05-shevet-design.md`

## Global Constraints

- Runtime: classic `<script>` tags from CDN only — globals `preact`, `preactHooks`, `preactSignalsCore`, `preactSignals`, `htm`. No bundler, no npm at runtime.
- Sheet ID: `1tRYgf0smzXqIrGJ1LdA-sF26Q3H8Zrw9zgbhrjlxZEM`, data tab name: `DB`, columns in order: `category, name, description, link, notes`.
- `dir="rtl" lang="he"` on `<html>`. Mobile-first; desktop = centered `max-width: 640px` column.
- Palette: bg `#faf5ec`, ink `#2d2a26`, accent `#c14e33`, muted `#8a7f6f`, hairline `#d8ccb8`.
- Fonts: **Rubik** (masthead/nav/headings/numbers), **Open Sans** (body) — Google Fonts with Hebrew subset.
- Category order: `מדריכים` first, `ספרים לקטנטנים` last (see `config.js` in Task 4).
- Tests run with `node --test tests/` — no test framework installs.
- Commit after every green step; commit messages end with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

### Task 1: Migration parser library (TDD)

Pure functions that turn raw gviz row arrays from the original tabs into flat product records, and serialize them to CSV.

**Files:**
- Create: `scripts/lib-migrate.mjs`
- Test: `tests/migrate.test.mjs`

**Interfaces:**
- Produces (ESM exports from `scripts/lib-migrate.mjs`), all consumed by Task 2:
  - `parseGviz(text: string) -> string[][]` — unwrap gviz JSONP, rows of stringified cells (`""` for null)
  - `parseStackedTab(rows: string[][]) -> Product[]` — the stacked-category product tabs
  - `parseGuidesTab(rows: string[][]) -> Product[]` — transposed guides tab
  - `parseBooksTab(rows: string[][]) -> Product[]` — one-column book list
  - `toCsv(products: Product[]) -> string` — header + RFC-4180-quoted rows
  - `Product = { category, name, description, link, notes }` (all strings, `""` when absent)

- [ ] **Step 1: Write the failing tests**

```js
// tests/migrate.test.mjs
import { test } from "node:test";
import assert from "node:assert/strict";
import {
  parseGviz, parseStackedTab, parseGuidesTab, parseBooksTab, toCsv,
} from "../scripts/lib-migrate.mjs";

const gvizWrap = (rows) =>
  `/*O_o*/\ngoogle.visualization.Query.setResponse(${JSON.stringify({
    version: "0.6", status: "ok",
    table: { cols: [], rows: rows.map((r) => ({ c: r.map((v) => (v == null ? null : { v })) })) },
  })});`;

test("parseGviz unwraps JSONP and stringifies cells", () => {
  const text = gvizWrap([["שלום", null, 5]]);
  assert.deepEqual(parseGviz(text), [["שלום", "", "5"]]);
});

test("parseStackedTab: category headers, column headers, products", () => {
  const rows = [
    ["", "", "", ""],
    ["🎒 קטגוריית מנשאים", "🎒 קטגוריית מנשאים", "🎒 קטגוריית מנשאים", "🎒 קטגוריית מנשאים"],
    ["שם המוצר / המותג", "תיאור והמלצות מהקבוצה", "לינק / מקור", "הערות ודגשים מיוחדים"],
    ["מנשא Neko Switch", "ילקוט פיזיולוגי.\nשורה שנייה", "lunalubabies.com", "-"],
    ["", "", "", ""],
    ["🍼 קטגוריית האכלה (בקבוקים, כוסות, מוצצים)", "", "", ""],
    ["שם המוצר / המותג", "תיאור", "לינק", "הערות"],
    ["בקבוק Avent", "מומלץ", "\\-", "אזהרה: לא לכולם"],
  ];
  const out = parseStackedTab(rows);
  assert.equal(out.length, 2);
  assert.deepEqual(out[0], {
    category: "מנשאים", name: "מנשא Neko Switch",
    description: "ילקוט פיזיולוגי.\nשורה שנייה", link: "lunalubabies.com", notes: "",
  });
  assert.deepEqual(out[1], {
    category: "האכלה", name: "בקבוק Avent",
    description: "מומלץ", link: "", notes: "אזהרה: לא לכולם",
  });
});

test("parseStackedTab skips rows before any category header", () => {
  assert.deepEqual(parseStackedTab([["מוצר יתום", "תיאור", "", ""]]), []);
});

test("parseGuidesTab transposes columns into guides", () => {
  const rows = [
    ["מדריך שינה 💤", "", "מדריך חיתולים 💩"],
    ["טקסט ארוך על שינה", "", "טקסט על חיתולים"],
  ];
  const out = parseGuidesTab(rows);
  assert.equal(out.length, 2);
  assert.deepEqual(out[0], {
    category: "מדריכים", name: "מדריך שינה 💤",
    description: "טקסט ארוך על שינה", link: "", notes: "",
  });
  assert.equal(out[1].name, "מדריך חיתולים 💩");
});

test("parseBooksTab skips title row and empty rows", () => {
  const rows = [
    ["רשימת ספרים מומלצים לקטנטנים 📚", "", "", ""],
    ["האריה שבפנים", "", "", ""],
    ["", "", "", ""],
    ["הזחל הרעב- אריק קרל", "", "", ""],
  ];
  const out = parseBooksTab(rows);
  assert.deepEqual(out.map((p) => p.name), ["האריה שבפנים", "הזחל הרעב- אריק קרל"]);
  assert.equal(out[0].category, "ספרים לקטנטנים");
  assert.equal(out[0].description, "");
});

test("toCsv quotes commas, quotes and newlines", () => {
  const csv = toCsv([{
    category: "מנשאים", name: 'מנשא "מיוחד", כן',
    description: "שורה 1\nשורה 2", link: "", notes: "",
  }]);
  const lines = csv.split("\r\n");
  assert.equal(lines[0], "category,name,description,link,notes");
  assert.ok(csv.includes('"מנשא ""מיוחד"", כן"'));
  assert.ok(csv.includes('"שורה 1\nשורה 2"'));
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/`
Expected: FAIL — `Cannot find module ... scripts/lib-migrate.mjs`

- [ ] **Step 3: Implement `scripts/lib-migrate.mjs`**

```js
// scripts/lib-migrate.mjs — pure parsing for the one-time sheet migration.

/** Map original long category headers -> clean site category names. */
const CATEGORY_MAP = {
  "מנשאים": "מנשאים",
  "עגלות, סלקלים וכיסאות בטיחות": "עגלות וסלקלים",
  "הנקה ושאיבה": "הנקה ושאיבה",
  "האכלה (בקבוקים, כוסות, מוצצים)": "האכלה",
  "טיפוח ואמבטיה (שמנים, סבונים, מגבונים)": "טיפוח ואמבטיה",
  "מוצרי שינה וניטור": "שינה וניטור",
  "ביגוד לתינוק": "ביגוד",
  "ציוד התפתחות ומשחק": "התפתחות ומשחק",
  "ריהוט וחדר הילד/ה": "ריהוט וחדר הילד",
};

export function parseGviz(text) {
  const json = JSON.parse(text.slice(text.indexOf("(") + 1, text.lastIndexOf(")")));
  return json.table.rows.map((r) =>
    (r.c || []).map((c) => (c && c.v != null ? String(c.v) : ""))
  );
}

/** "🎒 קטגוריית מנשאים" -> "מנשאים" (mapped through CATEGORY_MAP). */
export function stripCategory(header) {
  const stripped = header
    .replace(/[\p{Extended_Pictographic}️]/gu, "")
    .replace(/קטגוריית/g, "")
    .trim();
  return CATEGORY_MAP[stripped] || stripped;
}

/** "-", "\-" and whitespace-only are "no value". */
function cleanField(v) {
  const t = (v || "").trim();
  return t === "-" || t === "\\-" ? "" : t;
}

export function parseStackedTab(rows) {
  const products = [];
  let category = null;
  for (const cells of rows) {
    const vals = cells.map((c) => (c || "").trim());
    if (!vals.some(Boolean)) continue;
    if (vals[0].includes("קטגוריית")) { category = stripCategory(vals[0]); continue; }
    if (vals[0].startsWith("שם המוצר")) continue;
    if (!category || !vals[0]) continue;
    products.push({
      category, name: vals[0],
      description: cleanField(vals[1]), link: cleanField(vals[2]), notes: cleanField(vals[3]),
    });
  }
  return products;
}

export function parseGuidesTab(rows) {
  if (rows.length < 2) return [];
  const [titles, bodies] = rows;
  const out = [];
  for (let i = 0; i < titles.length; i++) {
    const name = (titles[i] || "").trim();
    if (!name) continue;
    out.push({
      category: "מדריכים", name,
      description: (bodies[i] || "").trim(), link: "", notes: "",
    });
  }
  return out;
}

export function parseBooksTab(rows) {
  return rows
    .map((cells) => (cells[0] || "").trim())
    .filter((name, i) => name && i > 0) // row 0 is the tab's title line
    .map((name) => ({
      category: "ספרים לקטנטנים", name, description: "", link: "", notes: "",
    }));
}

function csvEscape(v) {
  return /[",\n\r]/.test(v) ? `"${v.replace(/"/g, '""')}"` : v;
}

export function toCsv(products) {
  const header = "category,name,description,link,notes";
  const lines = products.map((p) =>
    [p.category, p.name, p.description, p.link, p.notes].map(csvEscape).join(",")
  );
  return [header, ...lines].join("\r\n") + "\r\n";
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/`
Expected: all tests PASS

- [ ] **Step 5: Commit**

```bash
git add scripts/lib-migrate.mjs tests/migrate.test.mjs
git commit -m "feat: migration parsers for stacked/guides/books tabs"
```

---

### Task 2: Build `data/db.csv` from the live sheet + user import checkpoint

**Files:**
- Create: `scripts/build-db-csv.mjs`
- Create (generated): `data/db.csv`

**Interfaces:**
- Consumes: all exports of `scripts/lib-migrate.mjs` (Task 1).
- Produces: `data/db.csv` — the file the user imports into the spreadsheet as the `DB` tab. Later tasks read the live `DB` tab, not this file.

- [ ] **Step 1: Write the fetch-and-emit script**

```js
// scripts/build-db-csv.mjs — one-time: flatten all spreadsheet tabs into data/db.csv.
// Usage: node scripts/build-db-csv.mjs
import { execSync } from "node:child_process";
import { writeFileSync, mkdirSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import {
  parseGviz, parseStackedTab, parseGuidesTab, parseBooksTab, toCsv,
} from "./lib-migrate.mjs";

const SHEET_ID = "1tRYgf0smzXqIrGJ1LdA-sF26Q3H8Zrw9zgbhrjlxZEM";
const BASE = `https://docs.google.com/spreadsheets/d/${SHEET_ID}`;

// Tab names live in the xlsx export's workbook.xml (public sheet, no auth).
async function listTabs() {
  const res = await fetch(`${BASE}/export?format=xlsx`);
  if (!res.ok) throw new Error(`xlsx export failed: ${res.status}`);
  const file = join(tmpdir(), "shevet-sheet.xlsx");
  writeFileSync(file, Buffer.from(await res.arrayBuffer()));
  const xml = execSync(`unzip -p ${JSON.stringify(file)} xl/workbook.xml`, { encoding: "utf8" });
  return [...xml.matchAll(/<sheet[^>]*name="([^"]+)"/g)].map((m) =>
    m[1].replace(/&amp;/g, "&").replace(/&quot;/g, '"').replace(/&lt;/g, "<").replace(/&gt;/g, ">")
  );
}

async function fetchRows(tab) {
  const url = `${BASE}/gviz/tq?tqx=out:json&headers=0&sheet=${encodeURIComponent(tab)}`;
  const res = await fetch(url);
  if (!res.ok) throw new Error(`gviz failed for "${tab}": ${res.status}`);
  return parseGviz(await res.text());
}

const tabs = await listTabs();
console.log("tabs:", tabs.join(" | "));

const products = [];
for (const tab of tabs) {
  if (tab === "DB") continue; // don't re-ingest our own output on re-runs
  const rows = await fetchRows(tab);
  let parsed;
  if (tab.includes("רשימת ספרים")) parsed = parseBooksTab(rows);
  else if (tab.includes("מדריכים מפי")) parsed = parseGuidesTab(rows);
  else parsed = parseStackedTab(rows);
  console.log(`  ${tab}: ${parsed.length} rows`);
  products.push(...parsed);
}

if (products.length < 50) throw new Error(`suspiciously few rows: ${products.length}`);
mkdirSync("data", { recursive: true });
writeFileSync("data/db.csv", toCsv(products));
console.log(`wrote data/db.csv — ${products.length} products, ${new Set(products.map((p) => p.category)).size} categories`);
```

- [ ] **Step 2: Run it and sanity-check**

Run: `node scripts/build-db-csv.mjs`
Expected: prints each tab with a row count, ends with `wrote data/db.csv — N products, M categories` where N ≈ 100–140 and M ≈ 11 (9 product categories + מדריכים + ספרים לקטנטנים).

Then eyeball: `head -5 data/db.csv` — header row + clean product rows, Hebrew intact, no `[merged]` junk, no `קטגוריית` prefix in the category column.

- [ ] **Step 3: Commit**

```bash
git add scripts/build-db-csv.mjs data/db.csv
git commit -m "feat: generate flat db.csv from live sheet tabs"
```

- [ ] **Step 4: USER CHECKPOINT — import the CSV into the sheet**

Stop and ask the user to (30 seconds, in Google Sheets):
1. Open the spreadsheet → **File → Import → Upload** → select `data/db.csv`
2. Import location: **Insert new sheet(s)** (do NOT replace)
3. Rename the new tab to exactly `DB`
4. Optional (recommended): select column A rows 2+ → Data → Data validation → dropdown from the 11 category values

- [ ] **Step 5: Verify the live DB tab is readable via gviz**

Run:
```bash
curl -s "https://docs.google.com/spreadsheets/d/1tRYgf0smzXqIrGJ1LdA-sF26Q3H8Zrw9zgbhrjlxZEM/gviz/tq?tqx=out:json&headers=1&sheet=DB" | head -c 400
```
Expected: `google.visualization.Query.setResponse({...` with `"label":"category"` (or the header row values) in `cols`, and `"status":"ok"`. If it returns `"error"` / HTML, the tab name or sharing is wrong — resolve with the user before continuing.

---

### Task 3: Site data library `lib.js` (TDD)

Browser + node dual-exposed pure functions used by the app.

**Files:**
- Create: `lib.js`
- Test: `tests/lib.test.js`

**Interfaces:**
- Produces (`window.ShevetLib` in browser, `module.exports` in node) — consumed by `app.js` (Tasks 4–8):
  - `parseGviz(text: string) -> string[][]`
  - `toProducts(rows: string[][]) -> Product[]` — maps columns A–E, cleans `-`/`\-`, drops rows with empty `name`
  - `normalizeLink(raw: string) -> string|null` — usable href or null
  - `hasWarning(text: string) -> boolean` — matches `אזהרה` / `לא מומלץ`
  - `hasDetails(p: Product) -> boolean` — description, notes, or usable/plain link present
  - `orderCategories(products, categoriesConfig, fallbackEmoji) -> {name, emoji, items: Product[]}[]`
  - `searchProducts(products, q) -> Product[]`

- [ ] **Step 1: Write the failing tests**

```js
// tests/lib.test.js
const { test } = require("node:test");
const assert = require("node:assert/strict");
const L = require("../lib.js");

const gvizWrap = (rows) =>
  `/*O_o*/\ngoogle.visualization.Query.setResponse(${JSON.stringify({
    table: { cols: [], rows: rows.map((r) => ({ c: r.map((v) => (v == null ? null : { v })) })) },
  })});`;

test("parseGviz + toProducts: cleans placeholders, drops nameless rows", () => {
  const text = gvizWrap([
    ["מנשאים", "מנשא Neko", "תיאור\nרב שורתי", "lunalubabies.com", "-"],
    ["מנשאים", "", "יתום", "", ""],
    ["ספרים לקטנטנים", "האריה שבפנים", "", "\\-", ""],
  ]);
  const products = L.toProducts(L.parseGviz(text));
  assert.equal(products.length, 2);
  assert.deepEqual(products[0], {
    category: "מנשאים", name: "מנשא Neko",
    description: "תיאור\nרב שורתי", link: "lunalubabies.com", notes: "",
  });
  assert.equal(products[1].link, "");
});

test("normalizeLink", () => {
  assert.equal(L.normalizeLink("https://a.co/x"), "https://a.co/x");
  assert.equal(L.normalizeLink("lunalubabies.com/product-page/x"), "https://lunalubabies.com/product-page/x");
  assert.equal(L.normalizeLink("econawa_il (אינסטגרם)"), null);
  assert.equal(L.normalizeLink(""), null);
  // first URL wins in multi-link cells
  assert.equal(L.normalizeLink("babykaring.com/topa-top/ ; topa-top.com"), "https://babykaring.com/topa-top/");
});

test("hasWarning", () => {
  assert.equal(L.hasWarning("אזהרה: דווח על פציעות"), true);
  assert.equal(L.hasWarning("צוין כלא מומלץ באמת"), true);
  assert.equal(L.hasWarning("מומלץ בחום"), false);
});

test("hasDetails: books are plain, products with any detail are tappable", () => {
  assert.equal(L.hasDetails({ description: "", link: "", notes: "" }), false);
  assert.equal(L.hasDetails({ description: "יש", link: "", notes: "" }), true);
  assert.equal(L.hasDetails({ description: "", link: "a.co", notes: "" }), true);
});

test("orderCategories: config order, unknown categories last with fallback emoji", () => {
  const cfg = [{ name: "מדריכים", emoji: "🧭" }, { name: "מנשאים", emoji: "🎒" }];
  const products = [
    { category: "מנשאים", name: "א" },
    { category: "קטגוריה חדשה", name: "ב" },
    { category: "מדריכים", name: "ג" },
  ];
  const out = L.orderCategories(products, cfg, "📦");
  assert.deepEqual(out.map((c) => c.name), ["מדריכים", "מנשאים", "קטגוריה חדשה"]);
  assert.equal(out[2].emoji, "📦");
  assert.equal(out[1].items.length, 1);
});

test("searchProducts: case-insensitive over name+description, empty query -> []", () => {
  const products = [
    { category: "מנשאים", name: "Neko Switch", description: "ילקוט" },
    { category: "האכלה", name: "בקבוק", description: "פטמה עגולה" },
  ];
  assert.equal(L.searchProducts(products, "neko").length, 1);
  assert.equal(L.searchProducts(products, "פטמה").length, 1);
  assert.equal(L.searchProducts(products, "  ").length, 0);
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `node --test tests/`
Expected: new tests FAIL (`Cannot find module '../lib.js'`), Task 1 tests still PASS.

- [ ] **Step 3: Implement `lib.js`**

```js
// lib.js — pure data functions. Exposed as window.ShevetLib (browser) and module.exports (node tests).
(function (global) {
  function parseGviz(text) {
    const json = JSON.parse(text.slice(text.indexOf("(") + 1, text.lastIndexOf(")")));
    return json.table.rows.map((r) =>
      (r.c || []).map((c) => (c && c.v != null ? String(c.v) : ""))
    );
  }

  function cleanField(v) {
    const t = (v || "").trim();
    return t === "-" || t === "\\-" ? "" : t;
  }

  function toProducts(rows) {
    return rows
      .map((cells) => ({
        category: cleanField(cells[0]),
        name: cleanField(cells[1]),
        description: cleanField(cells[2]),
        link: cleanField(cells[3]),
        notes: cleanField(cells[4]),
      }))
      .filter((p) => p.name);
  }

  function normalizeLink(raw) {
    if (!raw) return null;
    const first = raw.split(/[\s;,]+/)[0];
    if (/^https?:\/\//i.test(first)) return first;
    if (/^[\w-]+(\.[\w-]+)+(\/\S*)?$/i.test(first)) return "https://" + first;
    return null;
  }

  function hasWarning(text) {
    return /אזהרה|לא מומלץ/.test(text || "");
  }

  function hasDetails(p) {
    return Boolean(p.description || p.notes || p.link);
  }

  function orderCategories(products, categoriesConfig, fallbackEmoji) {
    const byName = new Map();
    for (const p of products) {
      if (!byName.has(p.category)) byName.set(p.category, []);
      byName.get(p.category).push(p);
    }
    const out = [];
    for (const c of categoriesConfig) {
      const items = byName.get(c.name);
      if (items) { out.push({ name: c.name, emoji: c.emoji, items }); byName.delete(c.name); }
    }
    for (const [name, items] of byName) out.push({ name, emoji: fallbackEmoji, items });
    return out;
  }

  function searchProducts(products, q) {
    const needle = (q || "").trim().toLowerCase();
    if (!needle) return [];
    return products.filter((p) =>
      (p.name + " " + (p.description || "")).toLowerCase().includes(needle)
    );
  }

  const api = { parseGviz, toProducts, normalizeLink, hasWarning, hasDetails, orderCategories, searchProducts };
  if (typeof module !== "undefined" && module.exports) module.exports = api;
  global.ShevetLib = api;
})(typeof window !== "undefined" ? window : globalThis);
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `node --test tests/`
Expected: all tests PASS (both files).

- [ ] **Step 5: Commit**

```bash
git add lib.js tests/lib.test.js
git commit -m "feat: site data library (gviz parse, hygiene, grouping, search)"
```

---

### Task 4: App shell — index.html, config.js, data loading, loading state

**Files:**
- Create: `index.html`
- Create: `config.js`
- Create: `style.css` (theme tokens + base only; component styles come with their tasks)
- Create: `app.js` (state + load + minimal render; components fleshed out in Tasks 5–8)

**Interfaces:**
- Consumes: `window.ShevetLib` (Task 3).
- Produces (module-level in `app.js`, used by all later tasks):
  - signals: `products` (Product[]), `status` (`"loading"|"ready"|"error"`), `route` (`{page:"home"}|{page:"category",category:string}`), `query` (string), `selected` (Product|null)
  - `load(): Promise<void>` — SWR fetch, `CACHE_KEY = "shevet-db-v1"`
  - `html` — htm bound to `preact.h`
  - `window.SHEVET_CONFIG` — `{ SHEET_ID, TAB, TITLE, KICKER, CATEGORIES: [{name, emoji}], FALLBACK_EMOJI }`

- [ ] **Step 1: Write `config.js`**

```js
// config.js — the only file to touch when categories change.
window.SHEVET_CONFIG = {
  SHEET_ID: "1tRYgf0smzXqIrGJ1LdA-sF26Q3H8Zrw9zgbhrjlxZEM",
  TAB: "DB",
  TITLE: "שבט",
  KICKER: "ההמלצות של הקבוצה",
  CATEGORIES: [
    { name: "מדריכים", emoji: "🧭" },
    { name: "מנשאים", emoji: "🎒" },
    { name: "עגלות וסלקלים", emoji: "🛒" },
    { name: "הנקה ושאיבה", emoji: "🤱" },
    { name: "האכלה", emoji: "🍼" },
    { name: "טיפוח ואמבטיה", emoji: "🛁" },
    { name: "שינה וניטור", emoji: "👶" },
    { name: "ביגוד", emoji: "👕" },
    { name: "התפתחות ומשחק", emoji: "🧸" },
    { name: "ריהוט וחדר הילד", emoji: "🛏️" },
    { name: "ספרים לקטנטנים", emoji: "📚" },
  ],
  FALLBACK_EMOJI: "📦",
};
```

- [ ] **Step 2: Write `index.html`**

```html
<!DOCTYPE html>
<html dir="rtl" lang="he">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>שבט · ההמלצות של הקבוצה</title>
  <meta name="description" content="מדריך הרכישות לתינוק — ההמלצות הכי טובות של הקבוצה, במקום אחד" />
  <meta property="og:title" content="שבט · ההמלצות של הקבוצה" />
  <meta property="og:description" content="מדריך הרכישות לתינוק — כל ההמלצות במקום אחד" />
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>👶</text></svg>" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600;900&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="style.css" />
</head>
<body>
  <div id="app"></div>
  <script src="https://cdn.jsdelivr.net/npm/preact@10.26.4/dist/preact.umd.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/preact@10.26.4/hooks/dist/hooks.umd.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@preact/signals-core@1.8.0/dist/signals-core.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@preact/signals@2.0.1/dist/signals.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/htm@3.1.1/dist/htm.umd.js"></script>
  <script src="config.js"></script>
  <script src="lib.js"></script>
  <script src="app.js"></script>
</body>
</html>
```

Note for the implementer: after loading the page once, verify in DevTools console that `preact`, `preactHooks`, `preactSignalsCore`, `preactSignals`, `htm` are all defined. If a UMD global name differs on these pinned versions, check `Object.keys(window)` and adjust the global references in `app.js` — do not switch to ESM imports.

- [ ] **Step 3: Write base `style.css`**

```css
:root {
  --bg: #faf5ec; --ink: #2d2a26; --accent: #c14e33;
  --muted: #8a7f6f; --hair: #d8ccb8; --card: #fff;
  --rubik: "Rubik", sans-serif; --open: "Open Sans", sans-serif;
}
* { box-sizing: border-box; }
body {
  margin: 0; background: var(--bg); color: var(--ink);
  font-family: var(--open); font-size: 16px; line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
.app { max-width: 640px; margin: 0 auto; padding: 20px 18px 48px; }
h1, h2, h3, .no, .back, button { font-family: var(--rubik); }
a { color: inherit; text-decoration: none; }
.rule { border-top: 1.5px solid var(--ink); margin: 14px 0; }
.center { min-height: 60vh; display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; gap: 8px; padding: 0 24px; }
```

- [ ] **Step 4: Write `app.js` — state, routing, SWR load, placeholder UI**

```js
// app.js — SHEVET application.
const { h, render } = preact;
const { signal } = preactSignals;
const html = htm.bind(h);
const L = window.ShevetLib;
const CFG = window.SHEVET_CONFIG;

// ---- state ----
const products = signal([]);
const status = signal("loading"); // loading | ready | error
const query = signal("");
const selected = signal(null); // product shown in the bottom sheet

function parseHash() {
  const hash = decodeURIComponent(location.hash.slice(1) || "/");
  const m = hash.match(/^\/c\/(.+)$/);
  return m ? { page: "category", category: m[1] } : { page: "home" };
}
const route = signal(parseHash());
window.addEventListener("hashchange", () => {
  route.value = parseHash();
  selected.value = null;
  window.scrollTo(0, 0);
});

// ---- data ----
const GVIZ_URL =
  `https://docs.google.com/spreadsheets/d/${CFG.SHEET_ID}` +
  `/gviz/tq?tqx=out:json&headers=1&sheet=${encodeURIComponent(CFG.TAB)}`;
const CACHE_KEY = "shevet-db-v1";

async function load() {
  const cached = localStorage.getItem(CACHE_KEY);
  if (cached) {
    try { products.value = JSON.parse(cached); status.value = "ready"; } catch { /* stale junk */ }
  }
  try {
    const res = await fetch(GVIZ_URL);
    const fresh = L.toProducts(L.parseGviz(await res.text()));
    if (!fresh.length) throw new Error("empty DB tab");
    products.value = fresh;
    status.value = "ready";
    localStorage.setItem(CACHE_KEY, JSON.stringify(fresh));
  } catch (err) {
    console.error("load failed:", err);
    if (status.value !== "ready") status.value = "error";
  }
}

// ---- components (placeholder bodies; Tasks 5-8 flesh these out) ----
function Loading() {
  return html`<div class="center"><p>טוען את ההמלצות…</p></div>`;
}

function ErrorScreen() {
  return html`
    <div class="center error">
      <h2>אופס 😅</h2>
      <p>לא הצלחנו לטעון את ההמלצות. בדקו את החיבור לאינטרנט ונסו שוב.</p>
      <button class="retry" onClick=${() => { status.value = "loading"; load(); }}>נסו שוב</button>
    </div>`;
}

function Home() {
  const cats = L.orderCategories(products.value, CFG.CATEGORIES, CFG.FALLBACK_EMOJI);
  return html`<pre dir="ltr">${JSON.stringify(cats.map((c) => c.name + ": " + c.items.length), null, 1)}</pre>`;
}

function CategoryPage() { return html`<p>category — Task 6</p>`; }
function BottomSheet() { return null; }

function App() {
  if (status.value === "loading") return html`<${Loading} />`;
  if (status.value === "error") return html`<${ErrorScreen} />`;
  const r = route.value;
  return html`
    <div class="app">
      ${r.page === "home"
        ? html`<${Home} />`
        : html`<${CategoryPage} name=${r.category} />`}
      ${selected.value && html`<${BottomSheet} product=${selected.value} />`}
    </div>`;
}

render(html`<${App} />`, document.getElementById("app"));
load();
```

- [ ] **Step 5: Verify against the live sheet**

Run: `python3 -m http.server 8090` (from repo root), open `http://localhost:8090`.
Expected: brief "טוען את ההמלצות…", then a JSON dump listing all 11 categories with plausible counts, **מדריכים first**. Console: no errors. Reload → data appears instantly (localStorage cache path).

- [ ] **Step 6: Commit**

```bash
git add index.html config.js style.css app.js
git commit -m "feat: app shell — CDN wiring, config, SWR sheet loading"
```

---

### Task 5: Home page — editorial masthead + category index

**Files:**
- Modify: `app.js` (replace placeholder `Home`, add `CategoryIndex`)
- Modify: `style.css` (append home styles)

**Interfaces:**
- Consumes: signals + `html` + `L.orderCategories` (Tasks 3–4).
- Produces: `Home` renders `CategoryIndex` when `query.value` is empty, `SearchResults` (Task 8 — until then, an empty `div`) otherwise.

- [ ] **Step 1: Replace the placeholder `Home` and add `CategoryIndex` in `app.js`**

```js
function Home() {
  const cats = L.orderCategories(products.value, CFG.CATEGORIES, CFG.FALLBACK_EMOJI);
  const q = query.value;
  return html`
    <header class="masthead">
      <h1>${CFG.TITLE}<span class="dot">.</span></h1>
      <p class="kicker">${CFG.KICKER} · ${products.value.length} המלצות</p>
      <div class="rule"></div>
      <input
        class="search" type="search" placeholder="🔍 חיפוש בכל המדריך…"
        value=${q} onInput=${(e) => (query.value = e.target.value)} />
    </header>
    ${q.trim() ? html`<${SearchResults} />` : html`<${CategoryIndex} cats=${cats} />`}
  `;
}

function CategoryIndex({ cats }) {
  return html`
    <ol class="index">
      ${cats.map((c, i) => html`
        <li key=${c.name}>
          <a class="idx-row" href=${"#/c/" + encodeURIComponent(c.name)}>
            <span class="no">${String(i + 1).padStart(2, "0")}</span>
            <span class="nm">${c.emoji} ${c.name}</span>
            <span class="ct">${c.items.length} המלצות</span>
          </a>
        </li>`)}
    </ol>`;
}

function SearchResults() { return html`<div></div>`; } // Task 8
```
(Remove the old placeholder `Home`; keep `CategoryPage`/`BottomSheet` placeholders.)

- [ ] **Step 2: Append home styles to `style.css`**

```css
/* masthead */
.masthead h1 { font-size: 44px; font-weight: 900; letter-spacing: -1px; margin: 0; line-height: 1; }
.masthead .dot { color: var(--accent); }
.kicker { font-size: 12px; letter-spacing: 2px; color: var(--muted); margin: 6px 0 0; }
.search {
  width: 100%; border: 0; border-bottom: 1.5px solid var(--ink); background: transparent;
  font: 15px var(--open); color: var(--ink); padding: 10px 2px; outline: none; border-radius: 0;
}
.search::placeholder { color: var(--muted); }

/* category index */
.index { list-style: none; margin: 10px 0 0; padding: 0; }
.idx-row {
  display: flex; align-items: baseline; gap: 14px;
  padding: 15px 2px; border-bottom: 0.5px solid var(--hair);
}
.idx-row:active { background: rgba(193, 78, 51, 0.06); }
.idx-row .no { color: var(--accent); font-size: 17px; min-width: 30px; }
.idx-row .nm { font-family: var(--rubik); font-weight: 600; font-size: 18px; flex: 1; }
.idx-row .ct { color: var(--muted); font-size: 12px; white-space: nowrap; }
```

- [ ] **Step 3: Verify in browser**

Reload `http://localhost:8090`.
Expected: שבט. masthead (Rubik 900, terracotta dot), kicker with total count, search underline field, numbered index — `01 🧭 מדריכים` first, `📚 ספרים לקטנטנים` last, terracotta numbers, hairline separators. Mobile viewport (DevTools, 375px): no horizontal scroll.

- [ ] **Step 4: Commit**

```bash
git add app.js style.css
git commit -m "feat: editorial home — masthead and numbered category index"
```

---

### Task 6: Category page — product cards + plain list items

**Files:**
- Modify: `app.js` (replace placeholder `CategoryPage`, add `ProductCard`)
- Modify: `style.css` (append card styles)

**Interfaces:**
- Consumes: `route`, `products`, `selected`, `L.hasDetails` (Tasks 3–4).
- Produces: `ProductCard({ product })` — also reused by search results (Task 8). Tapping a detailed card sets `selected.value = product`.

- [ ] **Step 1: Replace placeholder `CategoryPage`, add `ProductCard` in `app.js`**

```js
function CategoryPage({ name }) {
  const items = products.value.filter((p) => p.category === name);
  const cfg = CFG.CATEGORIES.find((c) => c.name === name);
  const emoji = cfg ? cfg.emoji : CFG.FALLBACK_EMOJI;
  return html`
    <header class="cat-head">
      <a class="back" href="#/">→ חזרה</a>
      <h2>${emoji} ${name}</h2>
      <p class="kicker">${items.length} המלצות</p>
      <div class="rule"></div>
    </header>
    ${items.length
      ? items.map((p) => html`<${ProductCard} product=${p} key=${p.name} />`)
      : html`<p class="empty">אין עדיין המלצות בקטגוריה הזו</p>`}
  `;
}

function ProductCard({ product: p }) {
  if (!L.hasDetails(p)) {
    return html`<div class="pcard plain"><b>${p.name}</b></div>`;
  }
  return html`
    <button class="pcard" onClick=${() => (selected.value = p)}>
      <b>${p.name}</b>
      ${p.description && html`<p class="desc clamp">${p.description}</p>`}
    </button>`;
}
```

- [ ] **Step 2: Append card styles to `style.css`**

```css
/* category page */
.cat-head h2 { font-size: 26px; font-weight: 600; margin: 10px 0 0; }
.back { color: var(--accent); font-size: 14px; font-weight: 500; }
.empty { color: var(--muted); text-align: center; padding: 32px 0; }

/* product cards */
.pcard {
  display: block; width: 100%; text-align: right;
  background: var(--card); border: 1px solid var(--hair); border-radius: 12px;
  padding: 14px 16px; margin: 0 0 10px; cursor: pointer;
  font-family: var(--open); color: var(--ink);
}
.pcard b { font-family: var(--rubik); font-weight: 600; font-size: 16.5px; display: block; }
.pcard .desc { color: #5c554b; font-size: 14px; margin: 4px 0 0; }
.pcard.plain { cursor: default; padding: 12px 16px; }
.clamp {
  display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical;
  overflow: hidden;
}
button.pcard:active { border-color: var(--accent); }
```

- [ ] **Step 3: Verify in browser**

From home tap `מנשאים` →
Expected: URL becomes `#/c/מנשאים`, header with emoji + count + back link, white cards with name + 2-line clamped description. `ספרים לקטנטנים` → plain items, no pointer cursor, clicking does nothing. Browser back returns home. Direct deep-link reload on `http://localhost:8090/#/c/מנשאים` lands on the category.

- [ ] **Step 4: Commit**

```bash
git add app.js style.css
git commit -m "feat: category pages with product cards and plain book list"
```

---

### Task 7: Bottom sheet detail view

**Files:**
- Modify: `app.js` (replace placeholder `BottomSheet`)
- Modify: `style.css` (append sheet styles)

**Interfaces:**
- Consumes: `selected` signal, `L.normalizeLink`, `L.hasWarning` (Tasks 3–4). Opens whenever `selected.value` is set (from `ProductCard`, later search results).
- Produces: `BottomSheet({ product })` rendered by `App` (already wired in Task 4).

- [ ] **Step 1: Replace placeholder `BottomSheet` in `app.js`**

```js
function BottomSheet({ product: p }) {
  const href = L.normalizeLink(p.link);
  const close = () => (selected.value = null);
  return html`
    <div class="backdrop" onClick=${close}>
      <div class="sheet" onClick=${(e) => e.stopPropagation()}>
        <div class="handle"></div>
        <button class="close" onClick=${close} aria-label="סגירה">✕</button>
        <h3>${p.name}</h3>
        <p class="chip">${p.category}</p>
        <div class="sheet-scroll">
          ${p.description && html`
            <p class=${"sheet-desc" + (L.hasWarning(p.description) ? " warn" : "")}>${p.description}</p>`}
          ${p.notes && html`
            <p class=${"sheet-notes" + (L.hasWarning(p.notes) ? " warn" : "")}>
              ${L.hasWarning(p.notes) ? "⚠️ " : "📌 "}${p.notes}</p>`}
          ${p.link && !href && html`<p class="sheet-notes">🔗 ${p.link}</p>`}
        </div>
        ${href && html`
          <a class="linkbtn" href=${href} target="_blank" rel="noopener">🔗 פתיחת קישור</a>`}
      </div>
    </div>`;
}
```

- [ ] **Step 2: Append sheet styles to `style.css`**

```css
/* bottom sheet */
.backdrop {
  position: fixed; inset: 0; background: rgba(45, 42, 38, 0.45);
  display: flex; align-items: flex-end; z-index: 10;
}
.sheet {
  background: var(--bg); width: 100%; max-width: 640px; margin: 0 auto;
  border-radius: 18px 18px 0 0; padding: 10px 20px 20px; position: relative;
  max-height: 85vh; max-height: 85dvh; display: flex; flex-direction: column;
  animation: rise 0.22s ease-out;
}
@keyframes rise { from { transform: translateY(40%); } to { transform: translateY(0); } }
.handle { width: 36px; height: 4px; border-radius: 4px; background: var(--hair); margin: 4px auto 12px; }
.sheet h3 { font-size: 21px; font-weight: 600; margin: 0 0 4px; padding-left: 32px; }
.sheet .chip {
  display: inline-block; font-size: 11.5px; color: var(--accent);
  border: 1px solid var(--accent); border-radius: 999px; padding: 1px 10px; margin: 0 0 10px;
}
.sheet .close {
  position: absolute; top: 14px; left: 14px; border: 0; background: transparent;
  font-size: 17px; color: var(--muted); cursor: pointer; padding: 6px;
}
.sheet-scroll { overflow-y: auto; flex: 1; }
.sheet-desc { white-space: pre-line; font-size: 15px; margin: 0 0 12px; }
.sheet-notes {
  white-space: pre-line; font-size: 14px; color: #6b6355;
  background: #fff; border-radius: 10px; padding: 10px 12px; margin: 0 0 12px;
}
.warn { background: #fbebe8; color: #8f2c18; border-radius: 10px; padding: 10px 12px; }
.linkbtn {
  display: block; text-align: center; background: var(--accent); color: #fff;
  font-family: var(--rubik); font-weight: 500; border-radius: 12px; padding: 13px; margin-top: 4px;
}
```

- [ ] **Step 3: Verify in browser**

Expected: tapping a product card slides the sheet up over a dimmed backdrop; full description with preserved line breaks; notes as 📌 card; products whose notes contain `אזהרה` (e.g. Owlet in שינה וניטור, or Medela Calma in האכלה) show a red ⚠️ callout; link button opens the product site in a new tab. Dismiss works via backdrop tap AND ✕. Open a guide from `מדריכים` → sheet caps at ~85% height and the text scrolls inside.

- [ ] **Step 4: Commit**

```bash
git add app.js style.css
git commit -m "feat: bottom-sheet product detail with warnings and link button"
```

---

### Task 8: Search

**Files:**
- Modify: `app.js` (replace placeholder `SearchResults`)
- Modify: `style.css` (append result-chip style)

**Interfaces:**
- Consumes: `query`, `products`, `selected`, `L.searchProducts`, `L.hasDetails` (Tasks 3–6).
- Produces: live results list on home; tapping a detailed result opens the bottom sheet.

- [ ] **Step 1: Replace placeholder `SearchResults` in `app.js`**

```js
function SearchResults() {
  const results = L.searchProducts(products.value, query.value);
  if (!results.length) return html`<p class="empty">לא נמצאו תוצאות 🤷‍♀️</p>`;
  return html`
    <div class="results">
      ${results.map((p) => html`
        <div key=${p.category + "|" + p.name} class="result-wrap">
          <${ProductCard} product=${p} />
          <span class="result-chip">${p.category}</span>
        </div>`)}
    </div>`;
}
```

- [ ] **Step 2: Append result styles to `style.css`**

```css
/* search results */
.results { margin-top: 14px; }
.result-wrap { position: relative; }
.result-chip {
  position: absolute; top: 12px; left: 14px; font-size: 10.5px;
  color: var(--muted); border: 1px solid var(--hair); border-radius: 999px; padding: 0 8px;
  background: var(--card); pointer-events: none;
}
.result-wrap .pcard b { padding-left: 90px; }
```

- [ ] **Step 3: Verify in browser**

Expected: typing `נשא` on home replaces the index with matching cards from multiple categories, each with a small category chip; matches on description text too (e.g. `גרמניה` finds Neko Switch); clearing restores the index; tapping a result opens its bottom sheet; nonsense query shows "לא נמצאו תוצאות".

- [ ] **Step 4: Run full test suite (regression)**

Run: `node --test tests/`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add app.js style.css
git commit -m "feat: live cross-category search on home"
```

---

### Task 9: Error screen polish + offline cache verification

**Files:**
- Modify: `app.js` (`ErrorScreen` copy — mention sheet sharing)
- Modify: `style.css` (retry button)

**Interfaces:**
- Consumes: `status` signal, `load()` (Task 4).

- [ ] **Step 1: Update `ErrorScreen` in `app.js`**

```js
function ErrorScreen() {
  return html`
    <div class="center error">
      <h2>אופס 😅</h2>
      <p>לא הצלחנו לטעון את ההמלצות.</p>
      <p class="kicker">בדקו את החיבור לאינטרנט, או שהגיליון עדיין משותף לצפייה.</p>
      <button class="retry" onClick=${() => { status.value = "loading"; load(); }}>נסו שוב</button>
    </div>`;
}
```

- [ ] **Step 2: Append retry style to `style.css`**

```css
.retry {
  background: var(--ink); color: var(--bg); border: 0; border-radius: 12px;
  font: 500 15px var(--rubik); padding: 12px 28px; margin-top: 10px; cursor: pointer;
}
```

- [ ] **Step 3: Verify all three data paths in browser**

1. **Error, no cache**: DevTools → Application → clear localStorage → Network tab → set "Offline" → reload → expected: error screen with retry. Set "Online", tap נסו שוב → home renders.
2. **Cache while offline**: load normally once, then set Offline and reload → expected: full site renders from cache, no error flash.
3. **SWR**: online reload → content visible immediately (cache), no layout jump when fresh data lands.

- [ ] **Step 4: Commit**

```bash
git add app.js style.css
git commit -m "feat: error screen with retry; verify offline cache paths"
```

---

### Task 10: Deploy to GitHub Pages

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: the complete working site (Tasks 4–9).
- Produces: live public URL `https://<gh-user>.github.io/shevet/`.

- [ ] **Step 1: Write `README.md`**

```markdown
# שבט · SHEVET

מדריך הרכישות לתינוק — ההמלצות של הקבוצה, במקום אחד.

Static RTL Hebrew site. **Database = Google Sheet** (`DB` tab) read live via the
public gviz endpoint — content updates need no deploy, just edit the sheet.

## Stack
Preact + htm + @preact/signals via CDN `<script>` tags. No build, no npm.

## Develop
python3 -m http.server 8090   # then open http://localhost:8090

## Test
node --test tests/

## Deploy
git push (GitHub Pages serves `main`).

## Data
- Edit content: add rows to the `DB` tab of the sheet (category, name, description, link, notes).
- New category: add a row value in the sheet + an entry in `config.js` CATEGORIES (order = display order).
- `scripts/build-db-csv.mjs` was the one-time migration that generated `data/db.csv` from the original tabs.
```

- [ ] **Step 2: Verify gh CLI is authenticated**

Run: `gh auth status`
Expected: logged in. If not, stop and ask the user to run `gh auth login`.

- [ ] **Step 3: Create the public repo and push**

```bash
git add README.md && git commit -m "docs: README"
gh repo create shevet --public --source=. --push
```
Expected: repo created, `main` pushed.

- [ ] **Step 4: Enable GitHub Pages on main**

```bash
gh api "repos/{owner}/shevet/pages" -X POST -f "source[branch]=main" -f "source[path]=/" \
  || gh api "repos/{owner}/shevet/pages" -X PUT -f "source[branch]=main" -f "source[path]=/"
```
Then poll until built:
```bash
gh api "repos/{owner}/shevet/pages" --jq .status   # -> "built" (may take ~1-2 min)
gh api "repos/{owner}/shevet/pages" --jq .html_url
```

- [ ] **Step 5: Verify the live site**

Open the `html_url` from Step 4.
Expected: identical behavior to localhost — home, category, sheet, search, live data. Check on an actual phone too (send the user the URL — this is also the moment they can share it with the group).

- [ ] **Step 6: Final commit if anything changed**

```bash
git status --short   # expect clean; commit + push any stragglers
```

---

## Self-Review Notes

- **Spec coverage**: data migration (T1–T2), gviz read + SWR + hygiene (T3–T4), home/index + masthead (T5), category page + plain books (T6), bottom sheet + warnings + 90vh scroll (T7), search (T8), error paths (T9), Pages deploy (T10). Category order pinning: `config.js` in T4. Deep links: verified in T6.
- **Known judgment call**: `hasDetails` counts a non-URL link cell (e.g. `econawa_il (אינסטגרם)`) as detail — it renders as plain text in the sheet, which is correct (the info is real, just not clickable).
- **CDN pins**: exact versions pinned in T4; global-name check note included since UMD global names are the one environment-sensitive assumption.
